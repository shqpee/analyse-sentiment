# ============================================================
# requirements-data.txt — Dépendances de l'API Données UNIQUEMENT
# ============================================================
# Image Docker légère : l'API Données n'utilise que FastAPI + SQLite (stdlib).
# Pas de PyTorch, pas de transformers, pas de libs ML → build rapide.

fastapi==0.111.0          # Framework API REST
uvicorn[standard]==0.30.1 # Serveur ASGI
pydantic==2.7.3           # Validation des données
python-multipart==0.0.9   # Formulaires FastAPI
prometheus-fastapi-instrumentator==7.0.0  # Métriques Prometheus (/metrics)
